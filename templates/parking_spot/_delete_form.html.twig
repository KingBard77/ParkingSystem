<form method="post" action="{{ path('app_parking_spot_delete', {'id': parking_spot.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ parking_spot.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
